# RCS QR Code Generator
Generates a scannable QR code that opens a pre-filled RCS message to a given Agent. Enter an \*\*RCS Agent ID\*\* (required) and a \*\*Message\*\* (optional, defaults to "Welcome"), optionally add a small logo to embed in the code, then download the result as a PNG.

# License 

See License.md

